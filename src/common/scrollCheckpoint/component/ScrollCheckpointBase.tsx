import { useSetScrollCheckpoint } from "../ScrollCheckpointContext";

interface ScrollCheckpointBaseProps{
    index: number
};

function ScrollCheckpointBase({ index }:ScrollCheckpointBaseProps){

    // const ref = useSetScrollCheckpoint(index);

    return(
        // <div ref={ref}/>
        <></>
    );
}
export default ScrollCheckpointBase;